# Dev + test tooling. Install with: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
openai>=1.30
ruff>=0.4
mypy>=1.9
# Offline model export only (AGPL). Not required to run the server.
# ultralytics>=8.3
